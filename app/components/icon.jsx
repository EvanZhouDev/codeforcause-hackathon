export default {
  Outlined: ({ name }) => {
    return <span className="material-symbols-outlined">{name}</span>;
  },
  Rounded: ({ name }) => {
    return <span className="material-symbols-rounded">{name}</span>;
  },
  Sharp: ({ name }) => {
    return <span className="material-symbols-sharp">{name}</span>;
  },
};
